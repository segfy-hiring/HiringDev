@model List<ChannelViewModel>

@{
    ViewData["Title"] = "Search - Channel";
}

<form>
  <div class="form-group row">
    <div class="col-sm-10">
      <input type="text" name="queryText" class="form-control" placeholder="Search Channels on Youtube">
    </div>
    <div class="col-sm-2">
        <button class="btn btn-primary" type="submit">Search</button>
    </div>
  </div>
</form>

@if(Model != null && Model.Count > 0)
{
    <table class=" table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Title</th>
                <th scope="col">Description</th>
                <th scope="col">Published At</th>
                <th scope="col">Channel Id</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (var item in Model)
                {
                    <tr>
                        <th scope="row">@item.Id</th>
                        <td>@item.Title</td>
                        <td>@item.Description</td>
                        <td>@item.PublishedAt</td>
                        <td>@item.ChannelId</td>
                        <td><a href='@string.Concat("https://www.youtube.com/channel/", item.ChannelId)' target="_blank">Go To Channel</a></td>
                    </tr>
                }
            }
        </tbody>
    </table>

}
