@model SkillTestSegfy.Web.Models.Home.YoutubeSearchModel
@{
    ViewData["Title"] = "Skill Test Segfy";
    Layout = "_Layout";
}
@using (Html.BeginForm())
{
    <div class="content">
        <div class="form-horizontal">
            <div class="card">
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-5">
                            <div class="form-group">
                                <div class="col-md-4">
                                    @Html.LabelFor(model => model.Term)
                                </div>
                                <div class="col-md-8">
                                    @Html.EditorFor(model => model.Term)
                                </div>
                            </div>
                            @*<div class="form-group">
                                <div class="col-md-4">
                                    @Html.NopLabelFor(model => model.CodigoBarra)
                                </div>
                                <div class="col-md-8">
                                    @Html.NopEditorFor(model => model.CodigoBarra)
                                </div>
                            </div>*@
                        </div>
                        <!--<div class="col-md-7">
                            <div class="form-group">
                                <div class="col-md-4">
                                    Html.NopLabelFor(model => model.Ativos)
                                </div>
                                <div class="col-md-8">
                                    Html.NopDropDownListFor(model => model.Ativos, Model.StatusDisponiveis)
                                </div>
                            </div>-->
                            @*O módulo fiscal sempre é habilitado, mas caso o usuário não tenha subido o certificado significa que o usuário não terminou de configurar o módulo fiscal.*@
                            <!--if (BiWorkContext.LojaAtual.ModuloFiscalAtivo && !string.IsNullOrWhiteSpace(BiWorkContext.LojaAtual.NumeroCertificado))
                            {
                                <div class="form-group">
                                    <div class="col-md-4">
                                        Html.NopLabelFor(model => model.ConfigNFCe)
                                    </div>
                                    <div class="col-md-8">
                                        Html.NopDropDownListFor(model => model.ConfigNFCe, Model.StatusConfigNFCeDisponiveis)
                                    </div>
                                </div>
                            }
                            <div class="form-group">
                                <div class="col-md-4">
                                    Html.NopLabelFor(model => model.CategoriaIds)
                                </div>
                                <div class="col-md-8">
                                    Html.EditorFor(model => model.CategoriaIds, new { SelectList = Model.CategoriasDisponiveis })
                                </div>
                            </div>
                        </div>-->
                    </div>
                    <div class="row">
                        <div class="col-md-7 col-md-offset-5">
                            <button type="submit" class="btn btn-primary">
                                <i class="fa fa-search"></i>
                                Buscar
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card" style="margin-top: 20px;">
                <div class="card-body">
                    @if (Model.Items != null && Model.Items.Any())
                    {
                        <div class="row" style="margin-top: calc((var(--bs-gutter-x) * .5) * -2);">
                            @foreach (var item in Model.Items)
                            {
                            <div class="col-12 col-md-6 col-lg-4 d-flex align-items-stretch" style="margin-top: calc((var(--bs-gutter-x) * .5) * 2);">
                                <div class="card" style="width: 100%;">
                                    <img src="@item.ThumbnailUrl" class="card-img-top" style=" height: 190px; object-fit: cover;">
                                    <div class="card-body d-flex flex-column">
                                        <h5 class="card-title" style="font-size: 16px;">
                                            <span class="badge bg-dark" style="font-size: 10px; vertical-align: 2px;">@item.Type</span>
                                            @item.Title
                                        </h5>
                                        <p class="card-text" style="font-size: 12px;">@item.Description</p>
                                        <a href="@item.Url" target="_blank" class="btn btn-primary mt-auto" style="width: fit-content; font-size: 14px;">@item.Action</a>
                                    </div>
                                </div>
                            </div>
                            }
                        </div>
                    }
                    else
                    {
                        <span style="color: darkgray;">@Model.Message</span>
                    }
                </div>
            </div>
        </div>
    </div>
}
